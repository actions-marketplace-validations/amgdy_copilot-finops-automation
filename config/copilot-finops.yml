# yaml-language-server: $schema=../schemas/v3/copilot-finops.schema.json
#
# Public-safe starter Copilot FinOps config (v3). v3 is budgets-first and has no
# enterprise field — pass your enterprise slug via the action's `enterprise`
# input. Add budgets in your private config repo before applying live.
#
# See config/copilot-finops.example.yml for a worked example of every scope.
version: 3
